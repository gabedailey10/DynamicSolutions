// This line is used for type checking and IntelliSense in some editors.
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  // Add this images configuration
  images: {
    domains: ['images.unsplash.com'],
  },
};

export default nextConfig;
